name: sq1_assignment

concurrency:
  group: $-$
  cancel-in-progress: true

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  semantic-pull-request:
    uses: VeryGoodOpenSource/victorcovi/.github/workflows/semantic_pull_request.yml@v1

  build:
    uses: VeryGoodOpenSource/victorcovi/.github/workflows/flutter_package.yml@v1
    with:
      flutter_channel: stable
      min_coverage: 10

  spell-check:
    uses: VeryGoodOpenSource/victorcovi/.github/workflows/spell_check.yml@v1
    with:
      includes: |
        **/*.md
      modified_files_only: false
